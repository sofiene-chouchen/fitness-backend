// src/repl_bindings.ts
function registerReplBindings(app, Repl) {
  Repl.addMethod(
    "loadI18n",
    async (repl) => {
      const key = "i18n";
      repl.server.context[key] = await app.container.make(key);
      repl.notify(
        `Loaded ${key} module. You can access it using the "${repl.colors.underline(key)}" variable`
      );
    },
    {
      description: 'Load "i18n" service into the REPL context'
    }
  );
}
export {
  registerReplBindings
};
//# sourceMappingURL=repl_bindings-7HNEPW7N.js.map